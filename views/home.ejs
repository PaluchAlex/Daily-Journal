
<!DOCTYPE html>
<html lang="en">
    <%- include('partials/header') %>  
    <body>
        <h1>Home</h1>
        <p><%= content %></p>

        <% listOfPosts.forEach(function(item,index){ %>
        
            <h1><%= item.title %></h1>
            <p><%= item.body.substring(0,100) + " ..." %> <a href="/posts/<%= item.title %>">Read more</a></p>
            
        
        <% }) %>
    </body>
    <%- include('partials/footer') %>  
</html>